# 버스 혼잡도 모니터링 프로젝트 기획서

2026-09-21 · @Someone

## Product Goal (제품 목표)

특정 버스 노선(예: 163번)의 실시간 위치·혼잡도 데이터를 5분 간격으로 수집해, "어느 정류장에서 몇 시쯤 만차가 시작되는가"를 시간대 × 정류장 히트맵으로 한눈에 보여주는 웹 서비스를 만든다.

노선 ID를 하드코딩하지 않고 여러 노선으로 확장 가능한 구조로 설계해, 특정 구간·시간대의 혼잡 패턴을 데이터로 증명하고 배차 개선 등 의사결정의 근거 자료로 활용할 수 있게 하는 것이 최종 목표다. 나아가 직접 5분 주기로 축적한 실시간 혼잡도 로그를 동일 요일·시간대 패턴으로 학습해, 5\~10분 단위로 혼잡도와 예상 탑승인원을 예측함으로써 "몇 분 일찍 나가면 앉아갈 수 있는지" 같은 실질적인 출퇴근 의사결정까지 지원하는 것을 목표로 한다.

## Features (기능)

**기본기능**

- 정류소 기준정보 수집·저장: 공공데이터포털(data.go.kr)의 '서울특별시\_노선정보조회 서비스'(ID 15000193, getStaionByRoute) 호출 → 노선 ID로 정류장 순번(seq)/정류소ID(station)/명칭(stationNm)/ARS번호(arsId)를 1회 적재
- 5분 주기 실시간 차량 위치·혼잡도·재차인원 수집: 공공데이터포털 '서울특별시\_버스위치정보조회 서비스'(ID 15000332, getBusPosByRtid)로 위치와 혼잡도 코드(congetion: 0없음/3여유/4보통/5혼잡/6매우혼잡)를, '국토교통부\_노선별 차내 재차인원'(ID 15142063)로 실제 탑승 인원수를 함께 수집 (스케줄러 기반)
- 수집 데이터 파싱·정제: 수집시각, 차량ID, 순번(sect\_ord), 정차/주행 상태, 혼잡도 코드 추출
- 시계열 로그 저장소: 정류소 마스터와 조인 가능한 구조
- 혼잡도 코드 → 숫자 척도 변환 (여유=3, 보통=4, 혼잡=5 등)
- 시간대별 구간 집계: 5/10분 단위 버케팅
- 정류장 × 시간대 히트맵 시각화 웹 페이지
- API 지연·타임아웃·빈 응답에 대한 내결함성 처리 (다음 주기로 안전하게 넘어감)
- 정류장·시간대별 실제 탑승 인원수 연동: 공공데이터포털의 '국토교통부\_노선별 차내 재차인원'(ID 15142063, STCIS 교통카드 빅데이터 기반)을 혼잡도 코드와 함께 저장 — 갱신 간격이 5분 단위에 못 미치면 서울 열린데이터광장 월별 통계로 보정하는 방식을 병행

**추가기능**

- 다중 노선 지원 및 노선 선택 UI (노선 ID를 배열/환경변수로 관리)
- 특정 정류장·시간대 혼잡 임계치 초과 시 알림(이메일/슬랙)
- 요일별/평일-주말 비교 뷰
- 직접 쌓은 5분 단위 실시간 혼잡도 로그를 동일 요일·시간대별로 학습해 5\~10분 단위로 혼잡도·대략적인 탑승인원을 예측 (이동평균/회귀 등, 몇 분 일찍 나가면 앉아갈 수 있는지 판단 지원)
- 관리자용 수집 상태 모니터링 대시보드 (수집 성공률, 최근 실패 로그)
- 데이터 CSV/이미지 내보내기

## Product Backlog (PBI)

| PBI ID | 제목 | 설명 | 우선순위 |
| --- | --- | --- | --- |
| PBI-01 | 정류소 마스터 구축 | 공공데이터포털 '서울특별시\_노선정보조회 서비스'(ID 15000193)의 getStaionByRoute로 노선 ID → 전체 정류장 목록(순번/ID/명칭/ARS) 조회 후 저장 | High |
| PBI-02 | 실시간 수집 스케쥴러 | 공공데이터포털 '서울특별시\_버스위치정보조회 서비스'(ID 15000332)의 getBusPosByRtid로 5분 주기 노선 차량 상태 일괄 수집(위치+혼잡도 congetion) | High |
| PBI-03 | 데이터 파싱/정제 | 수집시각, 차량ID, sect\_ord, 상태, 혼잡도 코드 추출 | High |
| PBI-04 | 내결함성 처리 | API 지연/타임아웃/빈 응답 시 안전하게 다음 주기로 진행 | High |
| PBI-05 | 시계열 스토리지 설계 | 로그+마스터 조인 구조, collected\_at/sect\_ord 인덱스 | High |
| PBI-06 | 혼잡도 척도 변환 | 문자 코드 → 숫자 척도 매핑 | Medium |
| PBI-07 | 시간 버켓 집계 API | 5/10분 단위 시간대별 집계 엔드포인트 | High |
| PBI-08 | 히트맵 시각화 페이지 | 정류장×시간대 2차원 히트맵 웹 UI | High |
| PBI-09 | 결측 구간 처리 정책 | 데이터 없는 슬롯 보간/빈칸 처리 | Medium |
| PBI-10 | 노선 선택 UI | 여러 노선 중 선택해 조회 | Medium |
| PBI-11 (추가) | 혼잡 임계치 알림 | 특정 조건 충족 시 알림 발송 | Low |
| PBI-12 (추가) | 관리자 모니터링 대시보드 | 수집 성공률/실패 로그 확인 | Low |
| PBI-13 | 실제 승하차인원 데이터 연동 | (보정용) 서울 열린데이터광장 '버스노선별 정류장별 시간대별 승하차 인원' CSV를 월 1회 수집·적재 — 재차인원 API(PBI-16)의 갱신 간격이 부족할 경우의 보정 데이터로 활용 (시간 단위 통계로 세밀한 5\~10분 예측용이 아닌 혼잡도-인원수 환산 보정용 참고자료) | High |
| PBI-14 | 혼잡도-실제인원 보정 | 시간대별 실제 평균 인원(배치 데이터)으로 혼잡도 코드를 보정해, 5\~10분 단위 실시간 혼잡도 코드에 대응하는 추정 인원수 산출 (정확한 숫자가 아닌 추정치) | Medium |
| PBI-15 (추가) | 탑승승객 수 예측 모델 | 직접 수집한 5분 단위 실시간 혼잡도 로그를 동일 요일·시간대 패턴으로 학습해 5\~10분 단위 혼잡도/탑승인원 예측 (이동평균/회귀, 최소 2\~3주 데이터 누적 후 신뢰도 확보) | Low |
| PBI-16 (최우선 스파이크) | 재차인원 API 검증 | 확인됨: 공공데이터포털(data.go.kr)에 '국토교통부\_노선별 차내 재차인원'(ID 15142063)이 실제 등록되어 있고, 다른 서울 버스 API들과 동일하게 자동승인으로 서비스키 발급 가능. 다만 응답 필드·갱신주기 상세 스펙은 공개 문서만으로 확인 불가(키 발급 후 열람 가능한 '활용자가이드 PDF' 참조 필요) — 163번 노선 샘플 호출로 직접 검증 필요 | High |

## 개발 Lifecycle

데이터 파이프라인(백엔드)과 시각화(프론트에드)를 병행하면서 반복적으로 검증하기 좋은 **애자일(Scrum 기반) 1주 스프린트** 방식을 적용한다. 위 Product Backlog의 PBI를 우선순위에 따라 스프린트 백로그로 가져와 스프린트 플래닝 → 개발 → 스프린트 리뷰/회고를 반복한다.

단계별로는 크게 세 단계로 나누어 진행한다.

1단계(데이터 파이프라인): PBI-01\~05를 중심으로 정류소 마스터 구축과 수집기를 먼저 완성해 실제 데이터가 쌓이는지 검증한다.

2단계(집계/시각화): PBI-06\~10으로 집계 API와 히트맵 페이지를 만들어 수집된 데이터를 눈으로 확인한다.

3단계(확장): PBI-11\~12 등 추가기능을 여유가 되는 대로 뒤에 붙인다.

매 스프린트 종료 시점에 동작하는 산출물(수집기 또는 페이지)을 데모하는 것을 원칙으로 하여, 진행 상황을 조기에 파악하고 방향을 수정할 수 있게 한다.

## 기술스택 (Technical Stack)

아래는 서버리스 아키텍처를 기본 전제로 제안하는 기본값이며, 팀 상황에 따라 조정할 수 있다.

**Front-end**: React(Next.js) + 히트맵/차트 라이브러리(D3.js 또는 Nivo, Recharts) + Tailwind CSS

**Back-end**: AWS Lambda(Python 또는 Node.js) + API Gateway, EventBridge Scheduler로 5분 주기 트리거. 초기 프로토타입 단계에서는 Node.js(Express) 또는 Python(FastAPI)로 로컬 개발 후 서버리스로 전환하는 방식도 가능

**DB**: 시계열 데이터 특성상 PostgreSQL(TimescaleDB 확장) 또는 완전 서버리스 구성 시 Amazon DynamoDB. 정류소 마스터처럼 정적인 데이터는 일반 RDB 테이블로 충분

**예산 제약 시**: 관리형 PostgreSQL(RDS)은 무료가 가입 후 12개월로 한정되므로, 계속 무료로 쓰려면 Supabase나 Neon의 무료 Postgres 티어를 고려. DynamoDB는 AWS Always Free(25GB, 영구 무료)라 더 확실하게 비용이 들지 않지만, 시간대별 평균 같은 집계 로직은 Lambda 코드로 직접 구현해야 함.

**개발 Tool**: VS Code, Postman(API 테스트), Docker(로컬 DB 컨테이너), Git

**AI 도구**: Claude Code / GitHub Copilot(코드 작성 보조), Claude/ChatGPT(API 문서 분석, 설계 검토, 문서화)

**데이터 분석/예측**: Python(pandas, NumPy)으로 실제 승하차인원 데이터를 가공하고, scikit-learn 또는 Prophet 등 시계열 예측 라이브러리로 시간대별 탑승승객 수를 예측

## 환경구축 예시

**소통을 위한 도구**: Slack 또는 Discord

**회의록 등 파일 저장소**: Notion 또는 Google Drive

**버전관리시스템**: Git & GitHub — main/develop + 기능별 feature 브랜치 전략, PR(Pull Request) 리뷰 기반 병합
